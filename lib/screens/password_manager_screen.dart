import 'package:flutter/material.dart';

class PasswordManagerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Password Manager')),
      body: Center(
        child: Text('Password Manager Screen'),
      ),
    );
  }
}
